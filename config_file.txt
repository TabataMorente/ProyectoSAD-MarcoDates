{
   "target": "score",                   // Columna objetivo del dataset que se va a predecir o usar de referencia
   "method": "bayes",                   // Algoritmo principal: knn, tree, bayes, forest, logistic
   "task": "clustering",                // Tipo de problema a resolver: classification, regression o clustering
   "evaluation": "macro",               // Métrica de evaluación multiclase: macro (clases valen igual) o micro

   "hyperparameters_knn": {             // Hiperparámetros para K-Vecinos más cercanos
       "k_range": [3, 10],              // IMPORTANTE: Aquí se pone un limite maximo y minimo de vecinos
       "step": 2,                       // Va de 2 en 2, o el numero que sea
       "p": [1, 2],                     // 1 = distancia Manhattan, 2 = distancia Euclidiana
       "weights": ["uniform", "distance"] // uniform: todos pesan igual. distance: los cercanos pesan más
   },

   "hyperparameters_tree": {            // Hiperparámetros para Árboles de Decisión
       "max_depth": [3, 5, 10],         // Profundidad maxima del arbol
       "criterio": ["gini", "entropy"]  // gini o entropy (fórmula para decidir cómo dividir ramas)
   },

   "hyperparameters_bayes": {           // Hiperparámetros para Naïve Bayes
       "var_smoothing": [1e-9, 1e-7, 1e-5], // Suavizado de varianza (evita fallo en divisiones por cero)
       "bayes_type": "multinomial",     // multinomial, bernoulli, gaussian, none
       "alpha": [0.1, 0.5, 1.0, 2.0]    // Suavizado de Laplace: suma un valor para evitar probabilidades del 0%
   },

   "hyperparameters_forest": {          // Hiperparámetros para Random Forest
       "n_estimators": [100, 200],      // Numero de arboles que tendra el bosque
       "max_depth": [3, 5, 10, 20, null], // Profundidad maxima del arbol (null = sin límite)
       "max_features": ["sqrt", "log2"] // Cuantas variables se muestran a cada arbol al azar
   },

   "hyperparameters_logistic": {        // Hiperparámetros para Regresión Logística
       "C": [0.01, 0.1, 1.0, 10.0],     // Inverso de regularización (valores altos confían más en el dataset)
       "solver": ["lbfgs"]              // Algoritmo matemático que busca el peso óptimo
   },

   "preprocessing": {                   // Opciones de limpieza y transformación de datos
       "drop_duplicates": true,         // true/false: Eliminar las filas que sean exactamente iguales
       "drop_columns": ["reviewId"],    // Columnas a eliminar completamente (ej. IDs que no sirven)
       "text_features": ["content"],    // Columnas de texto, por ejemplo un mensaje o reseña
       "text_process_method": "tf-idf", // tf-idf, one-hot, bow (Cómo convertir texto a números)
       "ngram_range": [1, 1],           // Rango de agrupación de palabras. [1,2] agrupa sueltas y pares
       "limite_palabras": "none",       // maximo de palabras a evaluar. 'none' usa todas
       "language": "english",           // Idioma para quitar stopwords (ej. the, is, at...)
       "categorical_features": ["gender"], // columnas categoriales: pasar a numeros
       "categorical_encoding": "one-hot", // Metodo para convertir categorías (one-hot o label)
       "boolean_features": [],          // columnas booleanas en formato texto
       "missing_values": "impute",      // impute o drop (eliminar filas nulas)
       "impute_strategy": "most_frequent", // mean, median, mode (most_frequent)
       "outliers": "clip",              // clip limita los valores atípicos para que no distorsionen
       "discretize_features": [],       // columnas numéricas a discretizar (ej. edad)
       "discretize_bins": 3,            // cuantos intervalos o categorias para discretizar
       "scaling": "min-max",            // max, max-min, z-score (normalizar datos numéricos)
       "sampling_strategy": "none",     // undersampling, oversampling, none (balanceo de clases)
       "sampling_ratio": {"0": 3000, "1":1000, "2": 2500}, // Forzar numero exacto de datos por clase
       "erroneous_values": {            // Tratamiento de valores absurdos
           "action": "impute",          // Qué hacer con ellos
           "rules": {}                  // Reglas concretas para detectarlos
       }
   },

  "prompt_engineering": {               // Configuración para Modelos de Lenguaje Locales (Ollama)
    "mode": "sentiment_analysis",       // Qué tarea se le pide hacer al modelo
    "model": "gemma2:2b",               // Nombre del modelo a utilizar
    "possible_answers": ["Worst", "Bad", "Neutral", "Good", "Best"], // Restringe las respuestas que puede dar
    "temperature": 0,                   // 0 = determinista (clasificación). Más alto = más creativo
    "repeat_penalty": 1.0,              // Castigo por repetición de palabras
    "num_predict": 1,                   // Número máximo de tokens/palabras que responderá
    "top_k": 10,                        // Límite de vocabulario (evita alucinaciones)
    "top_p": 0.5                        // Límite de vocabulario por probabilidad
  },

  "prompt_engineering" : {                                          // Configuración para Modelos de Lenguaje Locales (Ollama)
    "mode": "clasificacion",                                        // Qué tarea se le pide hacer al modelo "clasificacion" o "oversampling"
    "clasificacion": {
      "model": "qwen2.5:1.5b",                                      // Nombre del modelo a utilizar
      "possible_answers": ["positive","neutral","negative"],        // Lista con las respuestas que podrá dar el modelo
                                                                    // Hiperparámetro de inferencia
      "temperature": 0,                                             // temperatura: 0 => determinista; 1=>Aleatoriedad Alta
      "repeat_penalty": 1.0,                                        // repeat_penalty: 1=> sin penalización por repetir; 2=>penalización muy alta por repetir
      "num_predict": 8,                                             // num_predict: Longitud de respuesta del modelo
      "top_k": 10,                                                  // top_k: valor bajo=> respuestas más seguras; valor alto=>mayor diversidad
      "top_p": 0.5,                                                 // top_p: selección dinámica del conjunto de
                                                                    // palabras más pequeñas hasta alcanzar una probabilidad acumulada top_p
      "shots": [0,1,5,10],                                          // Lista de shots. Se hace un barrido por shots. Los ejemplos se extraen del dataset
      "split": ["First","Random","Stratified","Manual"],            // Por cada shot qué tipo de split debe hacer en el dataset.
      "manual_split_indexes": [[[1, 5], 9], [6,3,2]],               // En caso de incluir "Manual" en "split", se hace una lista de colecciones
                                                                    // de instancias en el dataset. Hay que hacer referencia al número de fila de la instancia.
                                                                    // Para hacer slicing crea dentro de una coleccion una dupla.
      "test_questions": {                                           // Extracción de instancias a predecir
        "split_size": 10,                                           // Número de instancias a predecir. Por cada instancia un barrido completo de shots.
        "split": "Stratified",                                      // Tipo de split. "Fisrt", "Random", "Stratified" o "Manual"
        "manual_split_indexes": [76,5753,6139,7275, 9989, 9955],    // Al elegir "Manual", se escribe los números de fila de las instancias que se quieren predecir.
      },
      "seed" : 42                                                   // Semilla que se usara para los splits
    },
    "oversampling" : {
      "model": "qwen2.5:1.5b",                                      // Nombre del modelo a utilizar
      "possible_answers": ["positive","neutral","negative"],
      "temperature": 0.4,
      "repeat_penalty": 1.2,
      "num_predict": 40,
      "top_k": 30,
      "top_p": 0.5,
      "shots": [0],
      "split": ["Random"],
      "seed" : 42
    }
  }

  "clustering_lda": {                   // Hiperparámetros de Topic Modeling (Latent Dirichlet Allocation)
       "n_components_range": [2, 3, 4, 5, 6, 7, 8, 9, 10], // Lista de cuántos "temas" a buscar (para gráfico codo)
       "learning_method": ["batch"],    // batch (procesa todos los datos a la vez) o online (lotes)
       "max_iter": [10, 20],            // Máximo de iteraciones del algoritmo matemático
       "doc_topic_prior": [null, 0.1, 0.5], // Alfa: cuántos temas distintos se espera por documento (null=auto)
       "topic_word_prior": [null, 0.1, 0.5] // Beta: cuántas palabras distintas componen cada tema (null=auto)
   }
}